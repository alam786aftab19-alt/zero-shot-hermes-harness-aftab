Given the user's question, the data analyst's reasoning, and the data schema, produce a step-by-step plan for querying the data and generating an appropriate visualization. The plan should:
1. Identify which tables to use and how to join them (if multiple).
2. Specify any aggregations (sum, count, avg, etc.).
3. Suggest a chart type suitable for the result.
4. Outline any data transformations needed before visualization.

Keep the plan clear and actionable.
